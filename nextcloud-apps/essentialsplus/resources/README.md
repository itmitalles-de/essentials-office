# Runtime resources

`scripts/install-essentialsplus-app.sh` copies the canonical repository-level
`office-modules.json` here before enabling the app. The generated copy is not a
second source of truth.
